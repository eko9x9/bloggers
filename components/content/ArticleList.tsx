import React, { ReactElement } from 'react'

function ArticleList(): ReactElement {
    return (
        <React.Fragment>
            <h3 className="text-center bordered">Latest Post</h3>
        </React.Fragment>
    )
}

export default ArticleList
